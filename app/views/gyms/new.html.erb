<% @page_title = "New Gym" %>

<div id="page-wrapper">
	<div class="graphs">
		<h3 class="blank1">Create new Gym</h3>
		<%= form_for(:gym, :url => {:action => 'create', :id => @gym.id}) do |f| %>

			<% @action = "new" %>

		    <%= render(:partial => "form", :locals => {:f => f}) %>
		    <div class="row">
		    	<div class="col-sm-8 col-sm-offset-2">
				    
				    <%= submit_tag("Submit", :class => "btn-primary btn") %>
				    <%= link_to("Cancel",{:controller => 'gyms', :action => 'index'}, :class => "btn-danger btn") %>
				</div>
			</div>
			<br/><br/>
		<% end %>
	</div>
</div>